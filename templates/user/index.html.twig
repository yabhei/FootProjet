{% extends 'base.html.twig' %}

{% block title %}
<script src="{{ asset('js/search.js') }}"></script>
Hello PlayerController!
{% endblock %}





{# templates/user/index.html.twig
    {{ form_start(searchForm, { 'attr': { 'id': 'search-form' } }) }}
    {{ form_row(searchForm.position) }}
    <button type="submit">Search</button>
    {{ form_end(searchForm) }} #}
    {% block body %}
    <div id="list_table" >
<div>
    <h1 style="display: inline-block;" class="list-players">List of players</h1>
    </div> 
    {# {{ form_start(form) }}
    {{ form_row(form.position) }}
    {{ form_row(form.search) }}
    {{ form_widget(form) }}
    {{ form_end(form) }} #}
    <div class="search">
        {# <select name="position" id="positions">
        {% for position in positions %}
        <option value="{{ position.title }}">{{ position.title }}</option>
        {% endfor %} #}
    {# </select> #}
        {# <input type="text" placeholder="Search By position" class="search_box"> #}
        {# <input type="submit" class="btn btn-primary" value="Search"> #}
        <a href="{{ path('search_user') }}" class="btn btn-primary">Search</a>
        
    </div>
<table class="table table-dark table-hover" >
    <thead> 
        <tr>    
            <th>Name</th>
            <th>Team</th>
            <th>Position</th>
        </tr>
    </thead>
    <tbody>
        {% for user in users %}
       

        <tr>
            <td><strong> <a href="{{path('details_user', {'id' : user.id})}}"> {{ user.firstname}} {{user.lastname }}
                    </a> </strong> </td>
                    {% if user.team %}
                        
                    <td> <a href="{{ path('details_team', {'id' : user.team.id }) }}"> {{ user.team }} </a> </td>
                    {% else %}
                    <td>

                        {{ "-" }}
                    </td>
                    {% endif %}
                    <td><strong>  {{ user.position}}
                     </strong> </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
</div>
{% endblock %}